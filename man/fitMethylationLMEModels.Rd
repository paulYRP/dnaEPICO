% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methylationLME_steps.R
\name{fitMethylationLMEModels}
\alias{fitMethylationLMEModels}
\title{Fit CpG-wise mixed-effects models for longitudinal methylation analyses}
\usage{
fitMethylationLMEModels(
  preparedData,
  nCores = 1L,
  libPath = NULL,
  lmeLibs = "lme4,lmerTest",
  correlationStructure = "none",
  correlationVar = NULL,
  omnibusTest = FALSE,
  omnibusDdf = "Satterthwaite",
  summaryDir = NULL,
  resumeFromSummary = TRUE,
  verbose = FALSE,
  logs = FALSE,
  log_dir = NULL,
  log_file = "log_methylationLME.txt"
)
}
\arguments{
\item{preparedData}{Object returned by \code{prepareMethylationLMEData()}.}

\item{nCores}{Integer. Maximum number of worker processes to use. Automatic
fitting uses the lme4 or nlme crossover and caps workers by available CpGs,
CPUs, and detected memory.}

\item{libPath}{Character vector or \code{NULL}. Optional library paths forwarded
to worker processes.}

\item{lmeLibs}{Character vector or comma-separated string of package names to
check on worker processes. The default is \code{'lme4,lmerTest'}.}

\item{correlationStructure}{Character. Residual correlation structure used
when \code{lmeLibs} selects \code{'nlme'}. One of \code{'none'}, \code{'AR1'}, or \code{'CAR1'}.}

\item{correlationVar}{Character or \code{NULL}. Variable used to order repeated
observations within \code{personVar} for \code{AR1} or \code{CAR1} residual correlation
structures. Must be supplied explicitly for \code{AR1} or \code{CAR1}.}

\item{omnibusTest}{Logical. If \code{TRUE}, calculate one joint fixed-effect test
per CpG for the phenotype-by-interaction term, or the phenotype main effect
when no interaction is requested.}

\item{omnibusDdf}{Character. Denominator degrees-of-freedom method used by
\code{lmerTest::contestMD()}: \code{'Satterthwaite'} or \code{'Kenward-Roger'}.}

\item{summaryDir}{Character or \code{NULL}. Directory used for one complete
compact summary per phenotype. \code{NULL} disables disk persistence.}

\item{resumeFromSummary}{Logical. If \code{TRUE}, reuse a complete summary in
\code{summaryDir} when it was generated from the same input file and model
configuration.}

\item{verbose}{Logical. If \code{TRUE}, emit progress messages with \code{message()}.}

\item{logs}{Logical. If \code{TRUE}, write the same messages to a log file.}

\item{log_dir}{Character or \code{NULL}. Directory used for the log file when
\code{logs = TRUE}.}

\item{log_file}{Character. File name used when \code{logs = TRUE}.}
}
\value{
A list with class \code{'dnaEPICO_methylationLME_models'}
containing compact coefficient matrices, unfiltered target summaries,
formulas, model conditions, omnibus results, hard errors, and phenotype
summary artifacts.
}
\description{
Fit one linear mixed-effects model per CpG for each phenotype requested in
the object returned by \code{prepareMethylationLMEData()}. Each native fit is
reduced to compact numerical results and discarded before the next batch is
returned.
}
\examples{
ex <- dnaEPICO:::exampleMethylationLMEStateDnaEpico()
model_results <- fitMethylationLMEModels(
  preparedData = ex$preparedData,
  nCores = 1,
  verbose = FALSE,
  logs = FALSE
)
names(model_results$fits)

}
