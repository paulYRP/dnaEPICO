% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocessingMinfiEwasWater_steps.R
\name{plotRawDensityMinfiEwasWater}
\alias{plotRawDensityMinfiEwasWater}
\title{Plot raw beta-value density from a raw preprocessing object}
\usage{
plotRawDensityMinfiEwasWater(
  rawData,
  targets,
  plotGroupVar = "Sex",
  display = FALSE,
  file = NULL,
  width = 2000L,
  height = 1000L,
  res = 150L,
  verbose = FALSE,
  logs = FALSE,
  log_dir = NULL,
  log_file = "log_plotRawDensityMinfiEwasWater.txt"
)
}
\arguments{
\item{rawData}{Object returned by \code{buildRawMinfiEwasWater()}.}

\item{targets}{Filtered phenotype data aligned with \code{rawData}.}

\item{plotGroupVar}{Character. Phenotype column used to group samples in the
density plot.}

\item{display}{Logical. If \code{TRUE}, draw the plot on the active graphics
device.}

\item{file}{Character or \code{NULL}. TIFF file written when supplied.}

\item{width}{Integer. TIFF width in pixels when \code{file} is supplied.}

\item{height}{Integer. TIFF height in pixels when \code{file} is supplied.}

\item{res}{Integer. TIFF resolution in DPI when \code{file} is supplied.}

\item{verbose}{Logical. If \code{TRUE}, emit progress messages with \code{message()}.}

\item{logs}{Logical. If \code{TRUE}, write the same messages to a log file.}

\item{log_dir}{Character or \code{NULL}. Directory used for the log file when
\code{logs = TRUE}.}

\item{log_file}{Character. File name used when \code{logs = TRUE}.}
}
\value{
Invisibly returns the saved TIFF path when \code{file} is supplied,
otherwise \code{NULL}.
}
\description{
Draw the pre-normalization beta density plot from a raw minfi object and a
grouping variable in the phenotype table.
}
\examples{
ex <- dnaEPICO:::exampleMinfiMetricsStateDnaEpico()
plotRawDensityMinfiEwasWater(
  rawData = ex$rawData,
  targets = ex$targets,
  plotGroupVar = "Sex",
  display = FALSE,
  verbose = FALSE,
  logs = FALSE
)

}
